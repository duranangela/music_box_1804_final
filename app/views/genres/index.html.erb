<h1>All Genres</h1>

<% @genres.each do |genre| %>
  <%= genre.name %><br>
<% end %>

<% if current_admin? %>
  <h3>Create a new Genre</h3>
  <%= form_for @genre do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit %>
  <% end %>
<% end %>
